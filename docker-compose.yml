web:
  build: .
  command: php -S 0.0.0.0:8000 -t /code
  ports:
    - "80:8000"
  links:
    - db
  volumes:
    - .:/code
  env_file: 
    - /.env
db:
  image: mysql:5.6
  environment:
    MYSQL_DATABASE: wechat
    MYSQL_ROOT_PASSWORD: 12345
